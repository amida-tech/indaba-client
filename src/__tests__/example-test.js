/* eslint-env jest */

describe('This is a sample test suite', () => {
    test('.. and this is a sample test', () => {
        const isSuccess = true;
        expect(isSuccess).toEqual(true);
    });
});
