import en from '../../i18n/en';

const initialState = {
  language: {
    choice: 'en',
    vocabulary: en
  }
};

export const SettingsReducer = (state = initialState, action) => {
    return state;
};
