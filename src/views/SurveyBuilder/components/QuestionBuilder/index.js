import React, { Component } from 'react';
// import PropTypes from 'prop-types';

class QuestionBuilder extends Component {
    render() {
        return (
            <div>
                Make other crap look nice first.
            </div>
        );
    }
}

export default QuestionBuilder;
